<h1>Listing maps</h1>

<table>
  <tr>
    <th>Name</th>
    <th>From mapping</th>
    <th>To mapping</th>
    <th>Mapping attribute</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @maps.each do |map| %>
  <tr>
    <td><%= map.name %></td>
    <td><%= map.from_mapping %></td>
    <td><%= map.to_mapping %></td>
    <td><%= map.mapping_attribute %></td>
    <td><%= link_to 'Show', map %></td>
    <td><%= link_to 'Edit', edit_map_path(map) %></td>
    <td><%= link_to 'Destroy', map, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Map', new_map_path %>
